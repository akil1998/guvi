#include<stdio.h>
#include<conio.h>
void main()
{
char a;
printf("enter the alphabet/n");
scanf("%d",&a);
if(a>=b && b<=z)
{
printf("the given word is alphabet");
}
else
{
printf("the given word is not alphabet");
}
}
